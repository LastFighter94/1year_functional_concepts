https://www.youtube.com/watch?v=ks8oftGP2oc

xук всегда должны начинаться со слова use

Во-вторых хук должны использоваться в функциональных компонентах, причем на высших уровнях

###############################################################

useInput

import {useState} from "react";

export default function useInput(initialValue) {
    const [value, setValue] = useState(initialValue);

    const onChange = e => {
        setValue(e.target.value)
    }

    return {
        value, onChange
    }
}

App

import React, {useState} from 'react';
import useInput from "./hooks/useInput";

const App = () => {
    const username = useInput('')
    const password = useInput('')

    return (
        <div>
            <input {...username} type="text" placeholder="username"/>
            <input {...password} type="text" placeholder="password"/>
            <button onClick={() => console.log(username.value, password.value)}>Click</button>
        </div>
    );
};

export default App;

БЕЗ ХУКА

Function App() {

const [value, setValue] = useState('');
const [password, setPassword] = useState('');

    const onChangeValue = e => {
        setValue(e.target.value)
    }

    const onChangePassword = e => {
        setPassword(e.target.value)
    }

    return (
        <div>
            <input value={value} onChange={() => onChangeValue} type="text" placeholder="username"/>
            <input value={password} onChange={() => onChangePassword} type="text" placeholder="password"/>
            <button onClick={() => console.log(value)}>Click</button>
        </div>
    );

}